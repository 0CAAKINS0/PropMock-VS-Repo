@model IEnumerable<PropMockModels.Product>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderStatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProductType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderStatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Order.ordernumber)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.filenumber">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.filenumber">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.filenumber">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
